# セッションログ

このプロジェクトでは `.claude/sessions/YYYY-MM-DD.md` に日付ごとの作業ログを残している(gitignore対象、個人用メモ)。

- セッション開始時、直近3日分のログが自動的にコンテキストに読み込まれる(SessionStart hook)。それを踏まえて、前回の続きであれば軽く触れてから作業を始めてよい。
- セッション終了時、Stop hookがそのセッションで一度だけ「今日やったことを書いて」とブロックしてくる。指示が来たら `.claude/sessions/YYYY-MM-DD.md`(今日の日付、無ければ新規作成)に、そのセッションでやったこと・決めたこと・次にやることを簡潔な箇条書きで追記してから、通常通り終了する。
- 箇条書きは日本語で、後から読んで何をしたか思い出せる粒度で書く(例: 「編集プロフィール機能のテストを追加」「TODOドキュメントの整理」など)。
