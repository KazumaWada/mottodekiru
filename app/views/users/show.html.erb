<div class="flex">

<%# 左カラムは一時的に無効化中(if falseで丸ごと非表示) %>
<% if false %>
  <!-- 左カラム（固定・sm以上で表示） -->
  <div class="hidden sm:block sm:w-[150px] fixed h-full">
    <%= render 'users/user_actions' %>
  </div>
<% end %>

  <!-- 右カラム（スマホでは余白なし） -->
  <div class="w-full max-w-3xl mx-auto">

    <% if @user.microposts.any? %>
      <% if current_user == @user %>
        <%# <%= render partial: 'users/profile' %>
        <%= render 'users/user_actions' %>
        <%= render partial: 'microposts/tags' %>
        <%#<%= render partial: 'microposts/content_navs_phone' %>
      <% end %>
      <%= render partial: 'microposts/micropost' %>
    <% end %>
  </div>

</div>




<script>


//tagsの値"dog, cat, hello, hhhhsss"
//,を2つまで検出し、あとは文字列の最後から取得。(ここは曖昧でいい。)

</script>



